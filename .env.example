# Base URL for the backend API.
# In local dev, leave unset (or use /api/v1) to route through the Vite dev proxy
# defined in vite.config.js. In production, set to the full backend API URL.
VITE_API_URL=/api/v1
